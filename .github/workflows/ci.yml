# push / PR ごとに build.yml の全ジョブ(ランナー × アーキ、全形式のときは Linux の形式別ジョブも)を並列に走らせる。
# PR は unpacked とスモークだけ、main への push と手動実行は全形式を Artifacts に 7 日間残す。
name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      full: ${{ github.event_name != 'pull_request' }}
      retention-days: 7
